# EMR Frontend

React + Vite + TypeScript + React Router + Axios + Tailwind.

## Setup

```bash
npm install
npm run dev
```

## Environment

Create `.env.local` from `.env.example`:

```bash
VITE_API_BASE_URL=http://localhost:8080/api
```
